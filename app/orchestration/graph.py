"""
LangGraph orchestration graph (stub).

The full LangGraph state machine is implemented in Phase 3.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def build_research_graph_stub() -> None:
    """
    Placeholder factory for the LangGraph workflow.

    Returns:
        ``None`` until Phase 3 wires a compiled graph object.
    """
    logger.info(
        "Research graph stub loaded",
        extra={"phase": "1"},
    )
    return None
